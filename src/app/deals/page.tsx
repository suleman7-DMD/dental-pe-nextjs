export default function DealsPage() {
  return (
    <div className="space-y-6">
      <div>
        <h1 className="text-2xl font-bold text-[var(--text-primary)]">
          Deal Flow
        </h1>
        <p className="mt-1 text-sm text-[var(--text-secondary)]">
          PE dental deal activity -- charts by year, state, deal type, and
          recent activity feed.
        </p>
      </div>
      <div className="flex h-60 items-center justify-center rounded-xl border border-[var(--border)] bg-[var(--bg-card)] text-sm text-[var(--text-muted)]">
        Deal Flow page -- to be built by page agent
      </div>
    </div>
  );
}
